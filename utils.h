#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

using namespace std;

vector<pair<string, string>> split_string(const string &str, const vector<string> &separators);

#endif // UTILS_H
